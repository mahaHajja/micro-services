spring:
  application:
    name: common-models
  cloud:
    config:
      uri: http://localhost:8888
server:
  port : 8083

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
