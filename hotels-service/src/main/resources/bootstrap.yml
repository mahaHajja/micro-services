spring:
  application:
    name: hotels-service
  cloud:
    config:
      uri: http://localhost:8888
server:
  port : 8082

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
